import 'package:flutter/material.dart';

class PromptListComponent extends StatelessWidget {
  const PromptListComponent({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    // throw UnimplementedError();
    return Text("aaaaa");
  }
}
